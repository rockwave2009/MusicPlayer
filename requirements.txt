# 专业音乐播放器依赖
# 注意：构建依赖在 GitHub Actions workflow 中单独安装

# GUI框架
PyQt6>=6.5.0

# 音频元数据
mutagen>=1.45.0

# 数值计算
numpy>=1.24.0

# macOS集成
pyobjc-framework-Cocoa>=9.0; sys_platform == 'darwin'
pyobjc-framework-Quartz>=9.0; sys_platform == 'darwin'

# 可选：在线搜索功能（需要先安装playwright浏览器）
# playwright>=1.40.0